---
name: 📦 Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  release:
    name: Create Release
    uses: N3b3x/hf-general-ci-tools/.github/workflows/ru-release.yml@main